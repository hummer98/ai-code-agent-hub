services:
  hub:
    build: .
    ports:
      - "3000:3000"
      - "4097-4200:4097-4200"
    volumes:
      - ${HUB_REPOS_PATH:-./.hub/repos}:/repos
    env_file:
      - .env
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 1G
